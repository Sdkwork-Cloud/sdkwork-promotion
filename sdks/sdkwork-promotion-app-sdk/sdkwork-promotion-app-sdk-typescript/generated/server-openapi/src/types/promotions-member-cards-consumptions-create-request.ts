/** Promotions member cards consumptions create request schema exposed by Cloud Router. */
export interface PromotionsMemberCardsConsumptionsCreateRequest {
  /** Amount field on promotions member cards consumptions create request. */
  amount: string;
  /** Source id field on promotions member cards consumptions create request. */
  sourceId?: string;
  /** Source type field on promotions member cards consumptions create request. */
  sourceType?: string;
}
