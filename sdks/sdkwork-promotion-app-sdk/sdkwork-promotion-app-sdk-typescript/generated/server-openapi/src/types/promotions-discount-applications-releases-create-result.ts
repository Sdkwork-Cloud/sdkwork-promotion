import type { NoData } from './no-data';

/** Promotions discount applications releases create result schema exposed by Cloud Router. */
export interface PromotionsDiscountApplicationsReleasesCreateResult {
  code: 0;
  data: unknown & NoData;
  /** Server-owned request correlation id. */
  traceId: string;
}
